# Creación de un espacio de trabajo en ROS 2

## 1. Crear el espacio de trabajo

Primero, se crea la carpeta del workspace y su estructura básica:

```bash
mkdir -p ~/ros2_ws/src

Luego, nos movemos a la carpeta principal del workspace:

```bash
cd ~/ros2_ws
